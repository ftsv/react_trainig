import { FiUser, FiGift } from 'react-icons/fi';

const LK = `${(<FiUser />)} Личный кабинет`;
const COUPON = `${(<FiGift />)} Личный кабинет`;

const MENU = {
    COUPON,
    LK,
};

export default MENU;
